name: Format
on:
  pull_request:
    branches: [master]
jobs:
  format_check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-node@v1
        with:
          node-version: "12.x"
      - name: Format
        run: |
          npm install -g yarn
          cd server
          yarn fmt:c
          cd ../web
          yarn fmt:c
