query SurveyResults($survey_id: Int!) {
  surveyResults(survey_id: $survey_id) {
    results {
      answerCount
      summaryStats {
        mean
        median
        mode
      }
      question
      qid
    }
    errors {
      ...StdFieldError
    }
  }
}
